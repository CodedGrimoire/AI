# Experiment Organization

This repo is organized into three experiment tracks:

- `experiments/all_algorithms`
- `experiments/weighted_astar_analysis`
- `experiments/heuristic_check`

Each track has:

- a dedicated `images/` folder for outputs
- two commands in its README: full Dhaka and 103384-node Dhaka subgraph
